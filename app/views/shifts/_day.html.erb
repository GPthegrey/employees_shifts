<div class="card">
    <div class="card-header">
      <h2><%= Date.today.beginning_of_month.advance(days: day - 1).strftime('%A, %B %d, %Y') %></h2>
    </div>
    <div class="card-body">
      <%= render 'shifts/shifts', shifts: shifts %>
    </div>
  </div>
