<div class="row justify-content-center d-none">
  <div class="col-md-6">
    <div class="card rounded-3 shadow mt-3 bg-primary">
      <div class="card-body">
        <%= simple_form_for(@assignment, url: refuerzo_assignments_path) do |f| %>
          <%= f.input :shift, label: 'Turno', collection: ['noche', 'mañana', 'tarde'], prompt: 'Seleccione el turno', input_html: { class: 'form-control bg-primary-dark border-primary-darker' } %>
          <%= f.input :employee, label: 'Empleado', collection: Employee.all.map { |employee| [employee.name, employee.id] }, prompt: 'Seleccione un empleado', input_html: { class: 'form-control bg-primary-dark border-primary-darker' } %>
          <%= f.button :submit, 'Crear', class: 'd-block w-50 m-auto btn btn-secondary-light border-primary-dark my-2' %>
        <% end %>
      </div>
    </div>
  </div>
</div>
